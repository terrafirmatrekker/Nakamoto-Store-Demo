/// <reference types="node" />
import { Networks as BNetworks, Opcode as BOpcode, Script as bScript } from "bitcore-lib";
/**
 * @dev Runs [[v1.createAddress]] and [[v2.createAddress]] side-by-side to make
 * sure that the v2 implementation is correct for all cases.
 * Eventually, v1 will be removed.
 */
export declare const createAddress: (addressToString: (bytes: Buffer) => string, networks?: typeof BNetworks | undefined, opcode?: typeof BOpcode | undefined, script?: typeof bScript | undefined) => (isTestnet: boolean, gGubKeyHash: Buffer, gHash: Buffer, prefix: Buffer) => string;
/**
 * @dev See [[createAddress]] comment.
 *
 */
export declare const pubKeyScript: (networks?: typeof BNetworks | undefined, opcode?: typeof BOpcode | undefined, script?: typeof bScript | undefined) => (isTestnet: boolean, gPubKey: Buffer, gHash: Buffer) => Buffer;
