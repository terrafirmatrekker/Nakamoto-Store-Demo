import { DepositCommon } from "@renproject/interfaces";
import { LockAndMint } from "@renproject/ren/build/main/lockAndMint";
import { MachineOptions } from "xstate";
import { GatewayMachineContext } from "../machines/mint";
export declare const renLockAndMint: <X>(context: GatewayMachineContext<X, any>) => Promise<LockAndMint<X, DepositCommon<X>, any, any, any>>;
export declare const buildMintConfig: <X>() => Partial<MachineOptions<GatewayMachineContext<X, any>, any>>;
